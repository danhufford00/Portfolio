#include <iostream>
using std::string;//Because one of our functions returns a string, the inpost file needs to know what a string is

#ifndef INPOST_H
#define INPOST_H

string convert(const string&);
int prec(char);

#endif
